import AuthResult from "./AuthResult";
import Jwt from "./Jwt";

export default interface IAuthApi {
    // static register(username: string, password: string): Promise<AuthResult>;
    // static login(username: string, password: string): AuthResult;
    // static authenticate(token: Jwt): AuthResult;
}